# Faifo Studio — DESIGN.md

Static, framework-free site (HTML/CSS/vanilla JS — no Node/build step). Built from the Faifo Studio brand guidelines (Edition 01, Aug.10.26) supplied as a pinned brief; the guideline deck's own "Website" slide (11/16) specified the landing-page structure directly.

## Current live state — temporary landing page

The site currently ships a trimmed version while the Auko case study, Clients, and Testimonials content isn't ready to publish: **Hero → Services → How we work → Contact** only. The full version (with Work: Auko, Clients, Testimonials, About) is preserved at the `full-site-v1` git tag for restoration once that content is ready — everything below this section describes that full build's design system, most of which still applies to the trimmed page (tokens, type, motion), except where noted.

Fixes applied on top of the trim:
- **Contact form was unreadable** — `.field input/textarea/select` used on-ink tokens (`--bone-50` text, `--ink-line-on-ink` borders) but the Contact section itself runs on-bone. Now uses `--ink-900` text and `--ink-line-on-bone` borders/chevron, matching the section it's actually in.
- **Contact email corrected** — `studio@faifo.studio` was wrong everywhere it appeared; replaced with `hello@faifostudio.com` (nav panel, contact meta-bar, form note, footer, `js/main.js` mailto handler).
- **Project type** select restored to the contact form (Photography / Cinematography / Brand identity / Something else — updated again when Services was restructured, see below), matching the Services list.
- **Availability card removed** from Contact per client request; `.availability-card` CSS deleted as dead code.
- **Motion polish** (Emil Kowalski's public "Animations on the Web" principles — no dedicated Claude skill for this was available, so his documented guidance was applied directly): `.reveal` tightened from 700ms/18px to 500ms/14px (duration should track the distance travelled, not linger); `.btn` gained a `translateY(-1px)` hover lift on top of the existing instant-press `:active` state, so buttons feel picked-up-then-pressed rather than just recoloring.

Fixes applied after an `impeccable:impeccable-finish-reviewer` pass on the trimmed page:
- **Contact layout rebalanced** — with the Availability card gone, `.enquire__row` left ~700px of dead space on wide viewports. Rebuilt as a two-column grid: copy (heading/description/"prefer email" line) on the left, the form itself on the right, stacking to one column at the existing 860px breakpoint.
- **The site's one brand-pinned offset shadow was orphaned** when the Availability card (its only owner) was removed. Re-homed onto `.btn.is-primary`, applied to the Contact form's "Send it →" submit button — the single most load-bearing action on this trimmed page.
- **Keyboard focus ring was being suppressed on all form fields** — `.field input/textarea/select:focus` set `outline:none` unconditionally, which (by specificity) killed the site-wide `:focus-visible` ring for keyboard users too, not just mouse clicks. Changed to `:focus:not(:focus-visible)` so the ring still shows for keyboard/tab navigation; mouse-click focus keeps just the border-color change.
- **Placeholder contrast bumped** from `rgba(14,14,12,.38)` to `.55` for better legibility against the bone ground.

**Hero: photo background swapped for a mouse trail (`js/main.js`, `.hero__trail*` in `css/style.css`).** The hero's static aerial photo and scrim are gone — the ground is now plain `--ink-900`. In their place, moving the mouse across the hero spawns a real Auko frame at the cursor (from a 12-image pool), pops it in (opacity/scale, 260ms), holds briefly, then fades it out (480ms) and removes it, capped at 5 concurrent images and throttled to one spawn per 110px of cursor travel so it reads as a trail, not a smear. Images sit at `z-index:1`, behind `.hero__content` (`z-index:2`), so the headline and CTAs are never obscured. Sized `width:200px; height:auto` — no `object-fit:cover`, consistent with the site's no-cropping rule. Gated to `(hover: hover) and (pointer: fine)` and off entirely under `prefers-reduced-motion`, so touch visitors and reduced-motion visitors just get the plain ink ground. CSS-only transitions (this is page chrome, not a gesture surface — no spring physics needed here, per the two-tier motion system below).

## Services restructure — three dedicated pages

The site went from a flat five-item Services list (real estate photography, videography, drone, brand design, brand media) to three consolidated services — **Photography** (stills, ground and aerial, merged with the old drone line), **Cinematography** (renamed from videography), **Brand identity** (merged brand design + brand media) — each with its own page: `photography.html`, `cinematography.html`, `brand-identity.html`.

Built from a project-local skill at `/Users/willkillen/Documents/Claude Skills/skills-main/skills/servicewebsite/SKILL.md` (not in this session's registered skill list, invoked by the user pointing directly at the file), which argues each service should be its own page rather than sharing a section, and lays out an 8-part structure: headline (service + audience), supporting paragraph, primary + secondary CTA, what's included, process, proof, FAQ, final CTA. All three pages follow that structure. Copy was drafted and passed through `anthropic-skills:humanizer` (no em dashes in prose, no AI-vocabulary words, no promotional language); `impeccable:impeccable`'s craft floor was read before editing UI — its bans on identical icon-cards and colored border-left accents shaped the layout choices below, and its explicit "pinned brief overrides" carve-out is why the site's brand-documented offset shadow and section-numbering convention were kept where they already existed.

- **Headlines split into two tiers** rather than one long compound line ("Real estate & property photography" as the big `h-section`, "For hotels, resorts & developers" as a smaller `.service-hero__audience` line below it) — a single run-on headline naming both service and audience wrapped unevenly at display size; splitting them reads more balanced and still satisfies the skill's "name the service and the audience" requirement.
- **What's included** reuses the existing `.dash-list` component (em-dash bullets, already on-brand) rather than a new checklist pattern.
- **Proof**: Photography's proof section revives the `.mosaic` masonry grid and the lightbox (`js/spring.js` + `js/lightbox.js`, re-included on this page only) with real Auko photos — genuine work, not fabricated. `js/lightbox.js`'s gallery selector was generalized from `#work .mosaic figure` to `.mosaic figure` so it isn't tied to the old (now-removed) Auko section id. Cinematography and Brand identity have no real proof yet — no video work exists, and no delivered brand-identity project exists in this project's assets — so both reuse the existing `.testimonial-empty` honest-placeholder pattern instead of inventing examples, per the user's explicit "add video soon, keep placeholders" instruction and this project's standing no-fabrication practice.
- **FAQ** is a hand-rolled accessible accordion (`.faq`, `<button aria-expanded>` + a `role`-less content region, not native `<details>`) so the open/close height can transition smoothly via a `grid-template-rows: 0fr → 1fr` trick — native `<details>` hides its content outside CSS's reach, so it can't be animated this way reliably across browsers. Toggle logic lives in `js/main.js`, shared across all pages.
- **Pricing**: the user chose to show pricing but didn't provide real numbers or a rate card. Every FAQ pricing answer on the three pages is deliberately factor-based ("scopes on property size / filming days / how much of the system you need," "send the basics and you'll get a straight scope and price back") rather than an invented number — **this still needs real figures from the user** to fully satisfy the skill's "at least a range or pricing factors" guidance.
- **Timelines** (process-step turnaround estimates — "5–7 business days," "1–2 weeks," etc.) were written as reasonable placeholders at the user's explicit instruction ("just make them up for now, I will edit later") and should be revisited once real numbers are settled.
- **Homepage Services section** (`#services` in `index.html`) links out to each dedicated page rather than carrying the full copy itself — matches the skill's guidance to keep one primary intent per page rather than blending. Hero subhead, hero meta line, and the Contact form's Project Type select were all updated to the three new service names; the Process section's "We shoot" step description was trimmed since it referenced the old category names directly. (Its visual treatment changed again below — `.service-overview` no longer exists.)

### Follow-up pass — web-interface-guidelines audit, apple-design motion audit, creative services display, cross-links

Three things done together after the initial three-page build shipped:

**`web-design-guidelines` skill (Vercel's Web Interface Guidelines) ran against all four pages.** Fixed: `[id] { scroll-margin-top: 88px; }` added globally — `.nav` is `position: sticky`, so every `#anchor` jump (nav links, hero CTAs, footer, the `photography.html#proof` cross-link) was landing with its target flush under the header before this. Every `<img>` sitewide (logo marks + the 9-photo mosaic) gained explicit `width`/`height` attributes matching each file's real intrinsic size (CLS prevention — the mosaic images keep their real, varying aspect ratios since nothing is cropped). `#f-email` got `spellcheck="false"`. Added `<meta name="theme-color" content="#0E0E0C">` to all four pages. Added `touch-action: manipulation` globally to `a, button` (removes the mobile double-tap-zoom delay). Added `text-wrap: balance` to `.h-display`/`.h-section` (only `.lead` had it before — headline widow prevention). Throttled `js/main.js`'s hero-trail `pointermove` handler, which was calling `getBoundingClientRect()` on every single move event (forced layout read, potentially 100+×/sec) — now the hero's rect is cached and only refreshed on `pointerenter`/`resize`. Noted but not changed: this guideline wants curly quotes, `humanizer` strips them to straight — no live conflict today since no quoted copy exists on the site, but a real choice if a testimonial or pull-quote goes in later.

**`anthropic-skills:apple-design` re-read and used to audit motion, not just as a build reference this time.** The original build (hero mouse-trail, lightbox, nav) already followed it closely — critically-damped springs with the right damping/response values, velocity handoff, momentum projection, rubber-banding, `prefers-reduced-transparency` handling on the translucent nav (`backdrop-filter: blur(20px) saturate(160%)`, drops to solid + no blur under reduced transparency), a soft gradient scroll-edge instead of a hard divider, size-specific letter-tracking already in `tokens.css` (`--tracking-display: -0.045em` large / `--tracking-meta: 0.14em` small, tightening further past 1400px) — all of that predates this pass and needed nothing. The real gaps were in components added in later turns, after the original pass: `.faq__q`, `.service-hero__back`, and the old `.service-overview__item` had no `:active` press-feedback, breaking the sitewide "respond on pointer-down" convention every other interactive element follows. Fixed by extending each with the same `transform: scale()` / `opacity` instant-press pattern already established elsewhere (`.nav__links a:active`, the shared icon-button press rule).

**Services display redesigned to be more visual, per explicit request** ("more creative way to display the services"). `.service-overview` (a compact text-only numbered list) is gone, replaced by `.service-features`: three large alternating rows (`.service-feature`), each pairing a big `h-section`-sized title with a real, uncropped Auko photo (`width:100%; height:auto`, no `object-fit:cover` — same no-cropping rule as the mosaic), image and text sides swapping per row (`nth-child(even)` order flip), stacking to one column under 860px. Photography's image is genuinely representative (a real estate interior shot). Cinematography and Brand identity use mood-appropriate Auko photography (a dusk exterior; the bamboo welcome pavilion) as atmosphere, not as a claim of specific delivered work for those services — the honest "coming soon" framing on each service page's own Proof section is what actually states what's real. A subtle `scale(1.03)` image zoom on hover/focus is the one added flourish, matching the mosaic's existing hover treatment.

**"Related services" cross-links added to all three service pages** (`.related-services`, two `.btn`-styled links each, pointing at the other two services), sitting between FAQ and the Final CTA. This directly follows the skill's own SEO section ("Internal links to related service pages"), which the initial build had missed — each service page dead-ended without ever mentioning the other two existed.

### Filmstrip gallery — Photography's Proof section, inspired by noho.ink

Direct request: "more premium feeling features... take inspiration from noho.ink... especially the gallery section." Inspected noho.ink's actual DOM rather than guessing from the screenshot — it's Webflow + GSAP + Lenis, with a `.gallery-collection` list (`display:flex`, `overflow-x` driven by a wheel listener, wider than the viewport) and a circular cursor-follow badge whose label swaps between "Scale" and "Close" depending on state. Reused image assets are one thing to copy; a smooth-scroll library and a bundled animation engine dropped into a zero-dependency vanilla site is another — so the *mechanism* was rebuilt from scratch to fit how this site already works, not vendored in.

`#proof`'s `.mosaic` grid (CSS-column masonry) became `.filmstrip` (`.mosaic filmstrip` — both classes stay on the same element so `js/lightbox.js`'s existing `.mosaic figure` selector keeps working unchanged): a `display:flex` row, `overflow-x:auto`, each figure sized by `height` with `width:auto` (real aspect ratio, still never cropped), bleeding past `.wrap`'s max-width to the section's own edges (`margin-inline: calc(-1 * var(--page-margin))` on `.filmstrip-wrap`) for a wider, more cinematic frame than the centered column everywhere else on the page.

Three pieces, matching what was asked for specifically:
- **Scroll** — native `overflow-x` is the accessible baseline (touch, trackpad, keyboard all just work unmodified); `js/main.js` adds a `wheel` listener that redirects vertical wheel input into `scrollLeft`, plus a thin `.filmstrip-progress__bar` tracking `scrollLeft / (scrollWidth - clientWidth)`.
- **Hover grows the photo** — `.filmstrip figure[tabindex]:hover { transform: scale(1.26); z-index: 3; }`, `transform-origin: bottom center` so it rises up rather than growing in all directions. The catch: `overflow-x:auto` forces `overflow-y` to compute as `auto` too (a CSS spec pairing, not a bug), so a scaled figure would get clipped at the container's edge — fixed with `padding-top: 16vh` reserved as headroom on `.filmstrip` itself, so growth has room to happen *inside* the scrollable box rather than needing to escape it. Disabled on `max-width:700px` (no real hover on touch, and no headroom reserved there either, so it would just clip).
- **Click still opens the existing lightbox** (`js/lightbox.js` + `js/spring.js`, spring-physics materialize/drag/rubber-band, built earlier this project) rather than rebuilding noho's own expand-in-place — it already covers the full viewport, which is a stronger version of "gets even larger to cover the text box above it," and it already had drag-to-page and momentum that noho's version doesn't attempt. No reason to replace something more capable with something less.
- **Custom cursor** — `.filmstrip-cursor`, a fixed-size circle absolutely positioned within `.filmstrip-wrap`, tracked via `pointermove` on the wrap and shown via `pointerenter`/`pointerleave` on each figure. Copy reads "Scale," matching noho's own label. Gated to `(hover: hover) and (pointer: fine)` plus `!prefers-reduced-motion` — the same gate the hero mouse-trail already uses — and hidden outright under 700px.

## Fullscreen intro + dedicated Contact page

Two additions, both requested together: a fullscreen logo entry animation for the homepage, and a proper Contact page (previously just an inline homepage section).

**Intro** (`.intro` in `style.css`, sequencing in `main.js`, markup + gate script in `index.html` only). Reference was serviceplusaquatics.com's entry animation — a full-viewport brand-colour curtain that shows the logo, holds, then lifts. Rebuilt as page chrome, not copied: the mark fades/scales in over `--ink-900` (the site's own dark ground, not the reference's black — the curtain then lifts into an already-on-ink hero, so there's no colour seam), holds briefly, then the whole curtain fades out. CSS-only transitions on `--ease-standard`, no bounce, matching the site's existing chrome tier — this isn't a gesture surface, so `js/spring.js` was never in consideration.

The logo itself is `assets/logo/mark-nav.png` (the same bone-on-transparent mark already used in the nav and footer) paired with real `Faifo Studio` text set in the site's own Archivo display style, not a flattened wordmark image — keeps it crisp, selectable, and pixel-consistent with every other instance of the brand name on the site.

Gating, in order of what actually decides whether it plays: an inline `<script>` in `index.html`'s `<head>` runs before paint and adds `html.show-intro` only when `sessionStorage` confirms this is a fresh session (`faifo-intro-seen` not yet set) and `prefers-reduced-motion` is off. The default state with no class present is `display:none` — a blocked or unavailable `sessionStorage`, JS disabled, or reduced-motion all degrade to "no intro" rather than a stuck fullscreen cover, same defensive posture as the hero mouse-trail's `canTrail` feature-detection. `main.js` only plays the sequence when `html.show-intro` is already present; it never decides whether to. Scoped to `index.html` only (the site's actual front door) — a direct deep-link into a service page never shows it, and once it has played in a session it won't replay even if the visitor later lands on the homepage from a service page, since the flag is already set.

One real bug surfaced and got fixed during the build: the state classes (`.intro.is-hiding`, `.intro.is-done`) were originally written without the `html.show-intro` prefix, so they had *lower* CSS specificity than the base `html.show-intro .intro` rule that sets `opacity:1`/`display:flex` — the fade-out and cleanup silently never applied, and the curtain would stay on screen forever after `is-done` was already in the DOM. Fixed by matching the prefix on every state selector. Caught by checking `getComputedStyle(...).display` against the DOM class list directly, since this session's Browser-pane tool renders a stale frame on any scrolled or timed-out screenshot (the same quirk noted in Finish status below) — a plain screenshot alone would have looked identical whether the bug was present or not.

**Contact page** (`contact.html`, new). Previously Contact was only an inline `#contact` section on the homepage. Inspiration was a general craft bar (an Awwwards "services" search — large confident type, real photography, restrained motion), not any single site's layout. Built as a fifth page-level artifact matching the exact skeleton the three service pages already established (`.service-hero`, meta-bar wayfinding, `.reveal` staggering, same nav/footer), not a new pattern:

- **Hero** — "Start a project," a response-time line in the same slot `.service-hero__audience` already occupies on service pages (repurposing an audience line into an expectation-setting line, same visual treatment), two CTAs (jump to the form, or email directly).
- **Enquiry form** — extends the homepage's existing two-column `.enquire__row` copy/form pattern rather than inventing a new one. Two fields added beyond the original name/email/project-type/message: company/property name (optional text), and a "Scope" select (single shoot / full package / ongoing retainer / not sure yet). Deliberately **not** a numeric budget-range select — the site has no real pricing figures anywhere (every FAQ answer sitewide is factor-based, "send the basics and you'll get a straight scope and price back"), and inventing dollar bands here would be exactly the kind of fabricated commercial claim the project has avoided from the start. The scope options extend that same factor-based philosophy into the form itself without inventing a number.
- **Reach us directly** — a `.media-row` (real photo + facts) reusing `.doctrine__item` for the three facts (email, studio location, response time). Originally used `welcome-01.jpg`; that file was deleted in a later asset refresh (see below) and the row now uses `assets/photo/personal/vietnam-bike-mountains.jpg` instead — still atmospheric, not a claim about the studio's own premises, and genuinely Vietnam, which fits a contact page for a Hoi An–based studio better than the original pavilion shot did anyway.
- **FAQ** — five contact-specific questions (what to include, response time, small-job policy, in-person meetings, "not sure which service") in the same plainspoken register as the other three pages' FAQs.
- No duplicate Final CTA section — the form above already is the page's destination, so a second CTA pointing back at itself would be padding, not substance.

**Homepage Contact section became a teaser**, mirroring exactly how Services already went from a full inline list to three cards linking to dedicated pages. `.enquire__row` (the full form) was replaced with `.contact-teaser` (headline, one line of copy, a single primary "Start a project →" button to `contact.html`, and the "prefer email?" mailto line kept in place so a quick email doesn't cost an extra click). All `#contact` / `index.html#contact` references sitewide — nav CTA, mobile nav panel, hero CTA, footer, and the three service pages' Final CTA buttons — now point to `contact.html` directly, the same link-out treatment Services already got.

**Tooling note:** this machine has no Node (confirmed again this session), so Impeccable's Node-script flows (`concept-seed.mjs`'s decision tournament, `detect.mjs`, `serve-question.mjs`) couldn't run. Both additions are local extensions of an already-established, DESIGN.md-documented world with strong existing structural precedent (the service-page pattern, the homepage teaser pattern), not open-ended new-surface work, so they were built directly against `craft-floor.md` rather than through the decision-tournament flow that's meant for genuinely open surfaces. No formal `impeccable-finish-reviewer` pass was run, for the same reason the previous round skipped it: this session's Browser-pane tool can't persist a screenshot to a file path the reviewer could read, and a review with no real screenshots is a review that can't check most of what it exists to check. Verification instead went through the accessibility tree (`read_page`), `document.documentElement.scrollWidth` overflow checks at 375px and 1440px, direct DOM/computed-style checks (`getComputedStyle`, class lists) in place of screenshots wherever the pane's scrolled-screenshot quirk made a capture unreliable, and functional checks (FAQ open/close, sessionStorage gating, link hrefs) run via `javascript_tool`. Recommend a real `impeccable-finish-reviewer` pass, with fresh screenshots, once a normal browser session confirms the pane issue doesn't apply there — same standing recommendation as the last round.

**Also discovered this session:** `faifostudio.com` is already live in production (a "seed" tab pinned to it appeared in the Browser pane unprompted). None of this session's changes are deployed there yet — this build only touched the local working copy.

## Services redesign, Auko film, asset refresh

Direct feedback: the homepage Services layout ("not unique or inspiring") needed a genuine redesign, not a polish — plus new photo assets to bring in, some old ones removed, and the existing (previously unreferenced) Auko walkthrough film needed a prominent home.

**Services became a scroll-pinned full-viewport sequence** (`.services-pin`, replacing `.service-features` entirely — old class removed, not deprecated in place). Researched genuine award-winning photography/portfolio sites on Awwwards for the *level of craft and interaction convention* — full-bleed pinned panels, huge display type, side progress rails — rather than cloning any single site's actual code, copy, or imagery; nothing here is a reproduction, it's an original build in the site's own established visual language (ink ground, Archivo display, `--ease-standard`, the same "no bounce for chrome" rule) aimed at that craft level.

Mechanism: `#services` is now a 300vh scroller (`.services-pin__scroller`) with a `position: sticky` stage (`.services-pin__sticky`) pinned at `top: 0` for the full 300vh of scroll. Three full-bleed panels sit absolutely stacked inside it, each a real Auko photo (`object-fit: cover`, a deliberate exception to the site's usual no-cropping rule — justified the same way the original photo hero was before it became the mouse-trail: this is atmospheric hero-scale imagery, not proof-gallery content, where the no-cropping rule actually lives) with a bottom scrim, a big number/label, an `h-display`-scale title, one line of copy, and a "Learn more" link. `main.js` computes scroll progress through the 300vh scroller (cached `getBoundingClientRect`, rAF-throttled scroll handler — same pattern as the hero trail's cached rect) and toggles which panel carries `.is-active`, cross-fading opacity and running a slow 6s linear Ken Burns zoom on the active image only. The sticky stage's `z-index` sits below the nav's, so the translucent nav keeps floating above the panels exactly as it already does over the hero — no special offset math needed. A side dot rail and a `01 / 03` counter track progress.

The sticky stage sits *under* the nav's own `position: sticky`, which only works because the nav has no separate scroll-margin claim on this section — verified by scrolling through and confirming the nav never gets pushed away or overlapped incorrectly.

**Mobile (≤860px) drops the pin entirely** — scroll-hijacking via `position: sticky` + scroll math fights mobile browser chrome (address bar show/hide changes `100vh` mid-scroll) and touch-scroll physics in ways that read as janky, not epic. Below the breakpoint, `.services-pin__sticky` becomes `position: static` and every panel is forced to `opacity: 1`, stacking as a plain sequence of ~86vh full-bleed blocks — same big-photo-big-type feel, no pinning. `main.js`'s scroll listener is gated behind `matchMedia('(min-width: 861px)')` so it never runs on mobile at all, though the CSS override would make it harmless either way.

**Auko walkthrough film** (`assets/video/auko-walkthrough.mp4`) now fills Cinematography's Proof section, replacing the honest "coming soon" placeholder — this resolves that gap with real footage rather than needing the placeholder any longer. `.film-feature` bleeds past `.wrap` the same way the photography filmstrip does. The file is 114MB with no `ffmpeg` on this machine to make a compressed proxy (the same constraint already flagged for the hero's storytelling reel), so it stays `preload="none"` with a poster frame (`overview-02-dusk.jpg`, the closest existing still — there's no way to extract a real frame from the video itself without ffmpeg either) and a custom circular play button; native `controls` are only added once the visitor actually clicks play, so there's no redundant double play-button between the custom control and the browser's native bar before that point. Nothing downloads until it's asked for.

**Asset refresh.** The user deleted eleven old Auko photos (`welcome-01/02`, `wellness-01/02`, `restaurant-01/02`, `texture-01/02`, `overview-03`, `lifestyle-03/04`) and added two new folders of personal photography (`Landscapes/`, `Portraits/`, mixed-case names with spaces — inconsistent with the site's existing lowercase kebab-case convention). Every broken reference left by the deletions got fixed: the homepage Services panels, `photography.html`'s proof gallery (three figures swapped for `lifestyle-05`, `lodge-ground-01`, `land-02`), Contact's facts-row photo, and the hero mouse-trail's image pool in `main.js` (three deleted filenames removed, three new ones added for variety). The new personal photography was copied — not moved, originals left untouched — into `assets/photo/personal/` with clean kebab-case filenames (`vietnam-bike-mountains.jpg`, `mountain-window.jpg`, `murmurations.jpg`, `waterfall.jpg`, `portrait-01` through `06`), and the four landscape originals (several MB each, one over 10MB) were downsampled with `sips --resampleWidth 2400` to match the weight of the site's existing "web-sized" Auko delivery, aspect ratio preserved exactly since the originals were already 3:2.

Only one of the new photos got used this round — `vietnam-bike-mountains.jpg` on Contact, replacing the deleted `welcome-01.jpg`. The rest (`mountain-window`, `murmurations`, `waterfall`, and all six portraits) are copied into `assets/photo/personal/` and ready, but weren't forced into a placement: they're general travel/portrait photography, not hospitality real-estate work, and the site's standing no-fabrication practice means they need an honest frame (a "the studio's eye" section, an About page) rather than being dropped into a slot that would imply they're client deliverables. No such section exists on the current trimmed homepage. Flagging this back to the user rather than inventing a home for them.

**New shared component:** replacing `.service-feature` (the old alternating-row services list) left Contact's "Reach us directly" row without the CSS it was built on — that row reused the same class for an unrelated photo-plus-facts layout. Rather than resurrecting services-specific styles for a non-services use, split out a generic `.media-row` (photo | body, two-column, hover zoom) that Contact now uses instead. `.service-feature` and its children are gone from `style.css` entirely.

Same tooling constraints as the last round: no Node, so built as a direct extension of the established world against `craft-floor.md` rather than through Impeccable's decision-tournament scripts; no formal `impeccable-finish-reviewer` pass for the same screenshot-persistence reason; verification went through the accessibility tree, `scrollWidth` overflow checks at 375/1440px, a scripted fetch-every-referenced-asset sweep across all five pages (confirmed zero broken references after the fixes above), and direct DOM/class-list checks in place of screenshots — the Browser pane's scrolled-screenshot quirk showed up again this round (confirmed via `document.visibilityState === "hidden"`, same root cause as before) and was worked around the same way.

### Follow-up — hero trail removed, Brand identity panel re-imaged

Two small direct requests right after the round above:

- **Hero mouse trail removed entirely**, not just disabled. `.hero__trail`/`.hero__trail-img` markup, CSS, and the whole `canTrail` block in `main.js` (image pool, spawn/retire, pointer listeners, cached-rect refresh) are gone. The hero is back to what fine-pointer visitors used to get as a bonus and what touch/reduced-motion visitors already got as the baseline: a plain `--ink-900` ground behind the headline. No replacement effect requested, so none was added.
- **Services' Brand identity panel** now uses `assets/photo/personal/mountain-window.jpg` (a mountain framed through a window opening) instead of `lodge-ground-01.jpg`. Picked over the other unused new photos for the panel specifically: its own composition is a frame-within-a-frame, which reads as a small conceptual echo of "identity/visual framing" for the one panel that isn't literally property photography, and its naturally darker lower half sits well under the panel's bottom scrim without fighting the overlaid title for contrast. Still atmospheric, not a claim that this is delivered brand-identity work — same standing as every other services-pin image.

### Follow-up — centered hero headline, new Statement section

**Hero recomposed.** The headline is now the brief's own original line, "Creative media for brands and spaces" (this exact wording was already sitting in the FIRST VIEWPORT block of the file's own opening direction-contract comment — the live copy had drifted from it at some earlier point), set without manual `<br>` line breaks so `text-wrap: balance` (already on `.h-display`) decides the wrap itself, and centered — both axes. `.hero__body` went from a bottom-aligned two-column row (heading+CTAs left, lead paragraph right) to a centered flex column (`align-items: center; text-align: center; margin: auto 0`), which also vertically centers it in the space between the top meta bar and the bottom scroll indicator now that there's no lead paragraph competing for that space. The lead paragraph itself didn't disappear — see below.

**New Statement section**, inserted between Hero and Services (`01 — What we believe`; Services, How we work, and Contact all shifted one number later, `02`/`03`/`04`). This is where the hero's old lead paragraph text went, rewritten to the user's supplied copy (one typo silently fixed, "Far to often" → "Far too often") and given real treatment rather than just relocated: each word of `We shoot, film, and design media that actually makes an impact on your business. Far too often do we see incredible projects settle for mediocre branding and visuals.` is its own `<span class="word">`, and `main.js` brightens them in a scroll-scrubbed cascade — dim (`opacity: 0.22`) to fully lit as the section passes through the viewport, driven by scroll position rather than a one-shot IntersectionObserver trigger, so the reveal pace is the visitor's own scroll pace, not a fixed animation duration. Mechanically this is the same cached-rect / rAF-throttled-scroll pattern as the services-pin panels and the hero trail before it, just computing a continuous per-word value instead of a discrete active-panel index. The CSS default (no JS, or `prefers-reduced-motion: reduce`) is every word already at `opacity: 1` — a complete, readable paragraph on its own; JS only ever dims words in on top of that baseline, so nothing degrades to half-legible if it fails to run.

New type treatment for this (`.statement__text`): mixed-case, not uppercase — the display type family (`h-display`/`h-section`) is uppercase by design for short headings, but a two-sentence statement set in all-caps at large size would fight its own readability, so this is regular sentence case, weight 500, 1.35 line-height, sized between `.lead` and `.h-section`.

**Local dev-server caching note, not a code issue:** verifying this round kept showing stale pre-edit CSS/JS (old alignment, empty word-opacity styles) even after confirming — by reading the files directly and by a cache-busted `fetch()` — that the edits were correctly saved and correctly served. Python's `http.server` sends no cache-control headers, so the Browser pane's own HTTP cache was serving old copies of `style.css`/`main.js` across reloads and even a fresh tab. Cache-busting the resource URLs directly (`?v=timestamp` on the stylesheet `href`, a cache-busted `fetch()` + inline `<script>` injection for `main.js`) confirmed both were correct all along. Worth remembering for any future round on this machine: a plain reload here is not sufficient evidence that a CSS/JS change didn't take effect.

**Dev server port conflict, separate from the caching note above:** a later `preview_start` attempt failed because a manually-started `python3 -m http.server 8934` from earlier in the session was still holding the port, and `.claude/launch.json`'s `faifo-studio` entry hardcoded `8934` in the run command, conflicting with the tool's own port assignment. Fixed by killing the stray process, writing a tiny wrapper (`serve_faifo.py`, outside this repo, in the session scratchpad) that reads the `PORT` env var instead of a hardcoded port, and setting `"autoPort": true`. Not a site issue, just a note in case this project's `launch.json` needs touching again.

### Follow-up — statement pacing, video reverted for deploy

Two more direct requests right after the round above:

- **Statement cascade tightened.** Feedback was that the scroll-scrubbed word reveal felt slow and not interesting enough. Two changes to `main.js`'s `updateStatement()`: the scroll range driving `progress` used to span `stTop - vh` to `stTop + stHeight` (roughly 1.5–1.8 screen-heights of scrolling, and it scaled with the text's own wrapped height, so longer statements would take even longer to resolve); it's now a fixed `stTop - vh*0.62` to `stTop + vh*0.05`, well under one screen-height regardless of content length, and per-word `windowSize` tightened from `2.4×` to `1.5×` so words feel more like they're individually arriving rather than blurring into a single wide wave. Second change, addressing "not interesting enough" specifically: dim words now also carry `blur(5px)` and `translateY(10px)`, resolving to sharp/in-place as they light up — a focus-pull on top of the brightening, not just an opacity fade. `craft-floor.md`'s own motion guidance calls this out directly ("reach past transform and opacity: blur... belong to the palette when they stay smooth"), so this isn't a new pattern for the site, just the first place it's earned.

- **Auko walkthrough video reverted to the "coming soon" placeholder** — not because the feature was wrong, but because deploying surfaced that `assets/video/` is gitignored (382MB total, and GitHub hard-rejects any single file over 100MB regardless) and the 114MB walkthrough file was never going to reach the live site through this repo. Shipping the HTML as it stood would have meant a poster image that fails silently when clicked — a broken feature on a public domain. `.film-feature` and its `.js` play-button logic are removed from `cinematography.html`/`style.css`/`main.js` entirely rather than left as dead code, matching this project's practice elsewhere (the Availability card CSS deletion, the Services rewrite) and DEPLOY.md's own precedent for the *original* hero video hitting the identical size problem: revert to the honest placeholder, restore once there's a real host for it (Cloudflare Stream, Mux, Vercel Blob, or a compressed proxy small enough for git). The code isn't lost — it's in this file's history above and in git history — so restoring it is a quick, known job whenever video hosting is sorted, not a rebuild.

**Bug found while verifying the round above, fixed same session:** the statement's last word ("visuals.") never fully reached full opacity/zero blur no matter how far you scrolled. Cause: `updateStatement()` mapped word `i`'s transition midpoint to `i / (n-1)`, so the very last word's midpoint landed at `t = 1` — exactly on `progress`'s own upper clamp. Since the transition is centered on `t`, a midpoint sitting on the boundary can only ever reach half its range (`raw = 0.5`, i.e. half-lit) before `progress` stops climbing. Same failure mode at the other end for the first word, just less noticeable. Fixed by remapping `t` into `[windowSize/2, 1-windowSize/2]` instead of the raw `[0,1]` spread, so both ends have room to fully resolve inside the valid progress range.

**This session's Browser-pane tool quirks, expanded:** two new ones on top of the visibilityState/rAF-throttling and stale-CSS-cache notes already on record above. First, the pane can report itself as `document.visibilityState === "hidden"` even immediately after being explicitly fronted via the tool's own tab-select action — fronting is not sufficient evidence the pane is "visible" from the page's own perspective, and scroll-driven rAF work will silently stall regardless. Second, the browser's HTTP cache is shared across tabs at the profile level, not per-tab — opening a fresh tab does *not* bypass a stale cached resource the way it seemed to earlier in the session (that was coincidence, not a real fix). The only reliable cache-bypass found this round was navigating directly to a cache-busted URL (`index.html?v=<value>`) rather than the plain path, for the top-level document itself as well as for individual CSS/JS resources.

### Follow-up — service pages taken down, video background on Services

Two more direct requests: keep the site minimal (no more dedicated per-service pages, for now), and give the Cinematography services-pin panel a moving video background instead of a static photo.

**`photography.html`, `cinematography.html`, and `brand-identity.html` deleted outright**, not just unlinked. Each Services panel changed from `<a href="…">` to a plain `<div>`: no `href`, no `.services-pin__link` "Learn more →" CTA, panel itself no longer interactive — just image/video, title, one line of copy. `js/spring.js` and `js/lightbox.js` (only ever included by the now-deleted `photography.html`) are left in place, unreferenced by anything — small, self-contained, and a known quick restore if the pages come back, so removing them bought nothing. Git history has all three pages verbatim if "for now" turns into "bring them back."

**Cinematography panel's background is now `assets/video/auko-walkthrough.mp4`**, not a static photo. Built deliberately *not* as a blind `autoplay` attribute (the first draft used one, then got corrected before shipping) — with `preload="none"` and playback triggered from `main.js` only once this specific panel's `.is-active` class is first set, gated behind `matchMedia('(min-width: 861px)')` (already required for the whole services-pin scroll mechanism) and `!prefers-reduced-motion`. Below that breakpoint, or under reduced-motion, nothing about the video ever loads — the `poster` attribute (`overview-02-dusk.jpg`) is all that ever shows, same honest-fallback shape as every other gated effect on this site. `.play()`'s promise rejection is swallowed (`.catch(() => {})`): if a browser's autoplay policy blocks it, the poster just stays put, no console noise, no broken state.

**Same GitHub file-size wall as the Cinematography-page video, hit again immediately.** `auko-walkthrough.mp4` is still gitignored, still 114MB, still over GitHub's 100MB hard limit — pushing this doesn't move the file, so the live site's version of this panel shows the poster image only, not moving, until the video has a real host. Flagged this before deploying rather than after, this time — user chose to ship both changes together anyway (poster-only on the video panel is an acceptable interim state; unlike the old play-button, there's no broken affordance visible, just a still image where a moving one is intended). Once video hosting exists, swapping the `<source>` here is the same quick job noted above for the Cinematography page's own proof section — both cost the same fix.

### Follow-up — single-page site, Services slides left, visual-craft pass

**The site is now genuinely one page.** `contact.html` is deleted; its content (extended enquiry form, "Reach us directly" facts, FAQ) is merged into the homepage's existing `#contact` section, which was previously just a teaser linking out. Every `contact.html` reference sitewide (nav, mobile panel, hero CTA, footer) reverted to `#contact`. Combined with the earlier removal of the three service pages, `index.html` is now the *only* HTML file in the project — there is nothing left to "take down" if asked again; any future multi-page request starts from here.

Structurally, the merge splits into three consecutive sections rather than one overloaded block: the numbered `04 — Contact` section carries the heading, lead copy, and the form itself; an un-numbered `on-ink` "Reach us directly" section carries the facts row (email/studio/response-time + the Vietnam landscape photo); an un-numbered `on-bone` "Questions" section carries the FAQ. None of the two trailing sections get their own top-level meta-bar number — they read as continuations of Contact, not new pillars, matching how "Related services" worked on the now-deleted service pages (a labeled cross-link block, not a numbered section). This also happens to restore ink/bone alternation through the tail of the page (Contact bone → Reach-us ink → Questions bone) that the trimmed structure would otherwise have lost.

**Services panels now slide left instead of cross-fading.** Rebuilt the transition mechanism, not just the timing: the three panels sit in a new `.services-pin__track` (`display:flex; width:300%`, each panel `width:33.33%`) instead of being absolutely stacked on top of each other. `main.js` sets `track.style.transform = translateX(-${progress * (n-1) * 100}vw)` every scroll tick — a continuous slide tied directly to scroll position, not a discrete swap — while `.is-active` (now chosen by `Math.round(progress * (n-1))`, nearest-panel rather than the old floor-based "which third am I in") still gates which panel's title/description text is visible and which image gets the Ken Burns zoom, so text doesn't visually overlap mid-slide. Mobile keeps the existing stacked fallback; `.services-pin__track` gets `transform: none !important` there so the inline desktop transform can never leak through.

**Dead code from the last two rounds' page deletions, cleaned up properly.** Deleting the three service pages left `js/spring.js` and `js/lightbox.js` (photography.html's gallery lightbox — spring physics, drag-to-dismiss, momentum paging) completely unreferenced; deleted outright rather than kept dormant, matching this project's standing practice. In `style.css`, a full audit against the one remaining HTML file found roughly 260 lines of now-orphaned rules and removed them: `.mosaic`/`.filmstrip*`/`.lightbox*` (the gallery), `.case-intro`/`.case-facts`/`.case-group` (the old full-site Auko case study, dead since before this session — never wired into the trimmed homepage to begin with, just never cleaned up), `.studio__row`, `.pull-quote`, `.clients__list`, `.testimonial-empty`, `.related-services`, `.service-hero*` (contact.html's and the service pages' shared hero treatment), `.blockquote-stat`, and `.services-pin__link` (the "Learn more" CTA styling, unused since panels stopped being links two rounds ago). Every removal was verified against `index.html` via `grep` before deleting, not assumed.

**Visual-craft pass, requested via `/taste-skill:high-end-visual-design`.** That skill's actual content is a generic Tailwind/React template — glass cards, `rounded-[2rem]` everything, pill buttons with nested icon circles, eyebrow-tag kickers, `backdrop-blur-2xl` — written for a different visual language entirely, and several of its mandates directly contradict this site's own pinned brand system (radius 0 everywhere is a documented brand pin; the skill bans plain 1px borders that this site's hairlines depend on; eyebrow kickers are something the site's own `impeccable` craft-floor pass explicitly banned in an earlier round). Applying it literally would have meant overwriting a brief-pinned identity with a generic one, not elevating it. Took the skill's underlying *goal* — genuine premium-agency polish, no generic-feeling defaults — and applied it through the site's own established idiom instead. Concretely, that meant a real audit pass (computed-style checks plus screenshots at both breakpoints) rather than a rewrite, which surfaced one real defect: `.enquire__row`'s `align-items: start` left the merged Contact section's copy column (138px, two paragraphs) stranded at the top of a 509px-tall row next to the full form, with a large dead gap below it — worse than in the original `contact.html`, where a now-removed heading had given that column more height. Fixed by changing to `align-items: center`, letting the shorter column sit centered against the taller one, a one-line fix but the actual defect this pass was for. A second hypothesis (mobile hero headline wrapping to four lines, tested via a `max-width` override) turned out to be a no-op — the viewport width was already the binding constraint, not the character cap — and on reflection, a tall multi-line display headline in generous negative space is consistent with this brand's own established "massive display type" identity, not a defect; the change was reverted rather than forced through.

### Follow-up — floating burger nav, redundant meta stripped, Services motion damped

Four direct requests: drop "Faifo Studio"/"Hoi An" repeating at the top of the hero, replace the full nav bar with a burger-only nav, stop repeating the service list in the Statement section's meta-bar, and fix the Services slide feeling "clunky" — the last one specifically via `anthropic-skills:apple-design`, the skill this project's own motion system (the now-retired lightbox's spring physics, the "no bounce for chrome" rule) was already built from.

**`.hero__top` (the "Faifo Studio" / "Hoi An, Vietnam" line) is gone from the hero entirely**, and the Statement section's meta-bar dropped its second span ("Photography · Cinematography · Brand identity") — both were repeating information already stated once (the nav-panel's own header, the hero's own lead copy history, the Services section itself) for no wayfinding gain. `hero__scroll`'s own "Photography · Cinematography · Brand identity" line is untouched — that one wasn't named, and it's doing different work (a footer-of-viewport summary, not a repeated header).

**The nav is no longer a bar.** `.nav` changed from a `position: sticky` full-width strip (background, backdrop-blur, wordmark, three inline text links) to `position: fixed` with two independent 44px frosted circles — the logo mark on the left, the burger on the right — floating over the page with no connecting background between them. The wordmark text is gone from the visible header (the link still carries `aria-label="Faifo Studio, home"` for a11y); `.nav__links` (the old inline Services/How it works/Start a project row) is deleted outright. The burger is no longer mobile-only — `.nav__toggle`'s old `display: none` default plus a `@media (max-width: 860px)` override that turned it on is gone; it's the *only* nav now, at every width, opening the same full-screen `.nav-panel` that already existed for mobile. The hamburger itself morphs into an X on open: three independent `<span>` bars (top/mid/bottom) instead of one static SVG glyph, each with its own `transform`/`opacity` transition (320ms, `--ease-standard`, no bounce) driven purely by the existing `aria-expanded` attribute — `openNav()`/`closeNav()` in `main.js` already toggled that attribute for a11y, so the morph needed zero new JS, only CSS keyed off `[aria-expanded="true"]`.

**Real bug found while building the above, fixed same pass:** the new circular-button styling was written against the bare `.nav__brand` class, which is also the class on the *nav-panel's own* internal logo+wordmark header (a different, non-circular context) — so opening the menu showed "Faifo Studio" squeezed into a 44px circle. Rescoped the circular treatment to `.nav .nav__brand` (descendant selector, main header only) and restored `.nav-panel .nav__brand`'s own flex/gap/font rules, which the original rewrite had deleted outright rather than reused. Caught by screenshotting the opened panel, not by reasoning about the selector alone — the failure mode (two contexts sharing one class name) doesn't announce itself until you look at both.

**Services track motion is now damped, not snapped.** Previously `track.style.transform` was set directly from scroll-derived `progress` on every scroll tick — a raw 1:1 coupling that reads as jerky against real (often discrete, wheel-notch) scroll input, which is almost certainly what "clunky" was describing. Consulted `apple-design` for the fix rather than guessing: the prescription is the same "follow, don't teleport" principle behind the retired lightbox's spring physics, applied here as a plain critically-damped lerp instead of a full spring (this is scroll-following chrome, not a dragged gesture surface, so no bounce is warranted or added — consistent with the site's existing two-tier motion system, not a new exception to it). Implementation: `update()` now only computes `targetX` and the discrete per-panel state (active class, dots, counter, video-play trigger); a separate `damp()` function runs its own `requestAnimationFrame` loop, moving `currentX` toward `targetX` by 14% of the remaining distance every frame and writing that to the track's transform, self-terminating once the gap drops under 0.05vw (so it isn't burning frames indefinitely at rest — restarts fresh on the next scroll event). `prefers-reduced-motion` skips the loop entirely and snaps `currentX` straight to `targetX`, matching how every other damped/spring effect on this site already degrades.

## Stack

- Plain HTML, CSS, JS. No framework, no bundler — this machine has no Node/npm installed, and a photography portfolio doesn't need one.
- Fonts self-hosted from the brand deck's own woff2 assets (`assets/fonts/`): Archivo (variable, wdth 62–125, weight 300–900) for display/body, IBM Plex Mono for meta/data.
- Logo mark and lockups extracted as real PNGs from the brand deck (`assets/logo/`) — not regenerated.

## Design tokens (`css/tokens.css`)

| Token | Value | Use |
|---|---|---|
| `--ink-900` | `#0E0E0C` | Dark ground |
| `--bone-100` | `#F2EEE6` | Light ground |
| `--bone-50` | `#FAF8F4` | Foreground-on-ink / lightest paper |
| `--bone-200` | `#E7E0D4` | Sunken |
| `--sand-300` | `#C9BCA8` | Fill / offset-shadow color |
| `--clay-500` | `#9C8465` | Accent type only |
| `--green-600` | `#14653A` | Signal — availability status only |
| `--rust-700` | `#9A3412` | Destructive/error only, never decorative |

Grid: 12 columns, 16px gutter, 1440px max width, page margin 24px → 48px at `lg`. Spacing scale is 2px-rooted: 2/4/8/16/24/32/48/64/96/128.

## Type scale

- Display (`h-display`, hero): Archivo 600, `wdth 112`, tracking −4.5%, line-height .9, uppercase, `clamp(2.75rem, 3.4vw+2rem, 7.4rem)`.
- Section (`h-section`): same voice, `clamp(2.25rem, 2.6vw+1.5rem, 4.6rem)`.
- Card heading (`h-card`): 600, tracking −3.5%, `clamp(1.5rem, 1.4vw+1rem, 2.1rem)`.
- Body (`body-text`): 17px/1.6, max 64ch.
- Meta (`meta` / `meta-sm`): IBM Plex Mono, uppercase, tracked +14%, 13px / 11px.

## Motion

Two tiers, deliberately different, per where each mandate applies:

- **Page chrome** (reveals, hovers, nav): CSS only. A single authored `.reveal` fade+rise (14px, 500ms, `cubic-bezier(.2,0,.2,1)`), staggered 70ms per sibling via `IntersectionObserver` in `js/main.js`. No spring/bounce here — the brand doc explicitly specifies "no bounce" for the studio's own UI. Hover/press states use 80–140ms transform/opacity only, with instant `:active` feedback on pointer-down (not on release) per Apple's response principle. iOS Safari needs a `touchstart` listener present anywhere in the document for `:active` to fire at all on tap — added in `js/main.js`.
- **The gallery lightbox** (a real gesture surface, not page chrome): vanilla spring physics in `js/spring.js` — Apple's damping/response model (WWDC18 *Designing Fluid Interfaces*), critically damped (`damping 1.0`) by default, bounce (`damping ~0.86`) reserved *only* for a release that was already moving — a flick gets a little overshoot, a tap-to-open never does. This is the one place bounce exists on the site, and it's earned by the interaction, not decorative.

`prefers-reduced-motion` disables both tiers — CSS via a blanket rule in `base.css`, springs via a `reducedMotion()` check in `spring.js` that snaps straight to the target instead of animating.

## Interaction layer — Auko gallery lightbox

Built from `anthropic-skills:apple-design` (WWDC *Designing Fluid Interfaces* translated to the web). Tap or Enter/Space on any Auko mosaic image opens a full-screen, uncropped viewer (`object-fit: contain`, not `cover` — the lightbox is where you see the whole frame, the mosaic thumbnails are the crops):

- **Materialize in/out** — scale + blur + opacity animate together as one spring, not a flat fade.
- **1:1 drag** — Pointer Events track the image to the finger exactly; axis-locked after a 10px hysteresis threshold (horizontal = page between photos, vertical = drag to dismiss), never both at once.
- **Momentum projection** — a flick doesn't just nudge the page; released velocity projects where the gesture *would* coast to (Apple's exponential-decay formula, not `v²/2a`), and that decides whether it pages or springs back.
- **Velocity handoff** — the release velocity becomes the spring's initial velocity, so there's no seam between the finger letting go and the animation continuing.
- **Interruptible** — grabbing the image mid-animation (mid-page-turn, mid-dismiss, mid-materialize) stops the running spring and starts the new one from its live on-screen value, never the logical target — no visible jump.
- **Rubber-banding** — dragging past the first or last photo resists progressively (Apple's rubberband formula) instead of hard-stopping or dragging forever.
- Keyboard (←/→/Esc), focus-trapped, focus returns to the thumbnail that opened it, `aria-modal`, per-image alt text carried through from the mosaic.

`js/spring.js` is a small (~120 line) dependency-free spring integrator — no Motion/Framer Motion, since there's no npm on this machine. Same physics, hand-rolled.

## Surfaces

Radius 0 everywhere. Exactly **one** offset shadow on the whole site (`box-shadow: 4px 4px 0 var(--sand-300)`), on the primary Contact form submit button (`.btn.is-primary`) — matches the brand doc's "one per page" rule, applied once sitewide since it's a single-page site. Re-homed here from the Availability card once that card was removed from the temporary landing page.

## Brief-pinned patterns kept despite generic anti-slop defaults

Two patterns a generic build would flag as AI-slop are here on purpose, because the brand guideline explicitly specifies them:

- **Section numbering** (`01 —`, `02 —` …) in every section's top meta bar — a documented brand convention, used as a wayfinding/context device across the whole 16-page guideline deck, not a decorative kicker glued above one headline.
- **The offset block shadow** — explicitly specified in the brand doc's own "Surfaces and states" page ("Offset ink shadow, 4px 4px 0. One per page."), used exactly once.

## Content structure

Rebuilt from a client-supplied handoff doc (services list, featured-project copy, process, clients, testimonials, about, contact) laid over the existing brief-pinned visual system. Sitemap of the old→new restructure: `planning/sitemap.canvas` (JSON Canvas). Section order:

1. **Hero** — "Creative media for brands and spaces," the real Auko hero film (`auko-storytelling.mp4`) autoplaying muted/looped as the background, poster falls back to the aerial still. Replaces the brand deck's fictional "Villa Sông, Hoi An" example — this was previously flagged as an open seam (fiction and the real Auko work sitting side by side); the handoff doc's hero rewrite resolves it, since the new copy makes no location-specific claim. Autoplay is gated to desktop + no `prefers-reduced-motion` (`js/main.js`) — mobile and reduced-motion visitors get the static poster only, since this is the full uncompressed 285MB reel, not a short compressed loop; see the deployment note below, this is the single biggest thing to fix before a real launch.
2. **Services** ("What we do") — five items (real estate photography, videography, drone, brand design, brand media) as a numbered editorial row list (`.numbered-list`), not cards — keeps the site's anti-card-grid discipline.
3. **Work: Auko** — the one portfolio section, real client work only. "Auko — Eco-Wellness Lodge, Phong Nha" (corrected from an earlier generic "glamping resort in an unnamed karst valley" framing once the real name and location were confirmed). Intro copy, a plainspoken "what we did" list, and one unified 17-photo masonry gallery (CSS columns, `height: auto`, no `aspect-ratio` box and no `object-fit: cover` anywhere in it) replacing what used to be four separate labeled sub-sections (land / lodges / welcome-wellness-restaurant / lifestyle) — same photos, one concise flowing gallery instead of four padded ones, and genuinely zero cropping: every tile shows the photo's real dimensions. Every image still opens in the interactive lightbox; figcaptions are kept in the DOM (the lightbox reads them) but visually hidden on the grid itself, so the tiles stay clean. The two down-page video embeds (storytelling reel, Open Way walkthrough) were removed once the storytelling reel became the hero background instead — no video player left mid-page. An honestly-marked pull-quote placeholder closes the section (no client quote exists yet). Photography from `/Users/willkillen/Downloads/Auko/__Full Shoot/Final Deliverables/_WEB SIZE/`.
4. **How we work** — four-step process, same numbered-row treatment as Services.
5. **Clients** ("Who we've worked with") — plain editorial text list (Auko, OTA Playhouse), not a logo wall — no logos exist for either, and the handoff doc explicitly prefers text over a mismatched logo grid.
6. **Testimonials** — zero real quotes exist. Built as an honest empty state (`.testimonial-empty`, dashed border, "Coming soon") rather than padded with fabricated quotes or three empty boxes that would read as broken.
7. **About** ("The studio") — merged the old separate Studio + Approach sections into one, per the handoff doc's "About/Philosophy" pattern; the old shooting-doctrine detail (grade, flash, delivery) survives as a compact supporting block. Old disciplines/subjects/also row dropped — Services now covers that ground.
8. **Contact** ("Start a project") — expanded form: added company/property name, project type and budget-range selects, alongside the existing name/email/message. Availability card (the one offset-shadow instance) unchanged.
9. **Footer** — tagline "Creative media for brands and spaces," nav repeat, unchanged location/email/copyright.

**Real content gaps, not fabricated:** an Instagram handle (the handoff doc marks this `[handle]`; omitted from the live page rather than guessed), testimonial quotes, and any logo/asset for OTA Playhouse. Contact email is `hello@faifostudio.com` (corrected from an earlier wrong `studio@faifo.studio`, and from the doc's unresolved `[email]` placeholder before that).

## Known deployment consideration

`assets/video/` is ~382MB (the real, uncompressed Auko exports — no `ffmpeg` on this machine to make web-weight proxies). This is now a bigger deal than it used to be: the hero background is `auko-storytelling.mp4` autoplaying on desktop, meaning every qualifying visitor's browser fetches the full 285MB reel just to load the homepage. It works locally because localhost has no real bandwidth constraint. **Before a real deploy, this file needs to become a short (5-15s), compressed (a few MB) H.264/H.265 loop purpose-built for a hero background** — not the full 1:50 delivery-quality edit. Either transcode it or move it to a video host (Cloudflare Stream, Mux, Vimeo) and swap the `<source>`. Shipping the current file as-is in production would tank LCP and burn real data on anyone's phone plan who happens to load it on a slow connection, even though it's gated off mobile.

The other Auko video, `auko-walkthrough.mp4` (Open Way cut), is currently unreferenced from the page — it was one of the two down-page video embeds removed once the storytelling reel moved to the hero. Real client footage, kept in `assets/video/` rather than deleted, in case there's a use for it later (a dedicated film page, a secondary embed, etc.).

## Imagery

- `assets/photo/auko/*.jpg`: real photography from the Auko shoot, used as delivered (already web-sized, no reprocessing needed). This is now the site's only photography — the earlier AI-generated placeholders (`hero-exterior.png`, `work-detail.png`, `texture-water.png`, used for the old fictional-villa hero and the old Approach section's illustrations) were deleted once the restructure removed every section that referenced them. No square crops anywhere on the site (mosaic tiles use 4:5/3:2/16:9; the lightbox uses `contain`, showing the full uncropped frame).

## Finish status

Structural QA done: full accessibility tree read (every section/heading/image-alt/form-field verified present and in order), all image/video assets confirmed served (200, correct byte sizes), zero horizontal overflow at 375px and 1440px, computed styles spot-checked. The lightbox's open/close/page-forward/page-back were each verified functionally correct (via click and keyboard, with proper focus return and full cleanup on close) — confirmed by checking resulting DOM/ARIA state rather than eyeballing it, since this session's Browser-pane tool had two recurring quirks unrelated to the page itself: screenshots of any scrolled position render blank (top-of-page screenshots are fine), and the tab periodically reports `document.visibilityState: "hidden"`, which throttles `requestAnimationFrame` and made the springs *look* broken (stuck, wrong direction) until accounting for the delay — they weren't; a normally-foregrounded tab runs them at full rate with no such lag. No formal `impeccable-finish-reviewer` pass was run this round for the same reason; recommend one before shipping, with fresh screenshots once a normal (non-automated) browser session confirms the pane issue doesn't apply there.
